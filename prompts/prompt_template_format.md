对以下客户对话进行标签分类。每段对话包含多条消息，请判断每段对话是否命中以下标签，若命中则给出确定依据（哪条消息）。


# 标签定义：
{{labels_text}}

# 输出:
- 直接输出 JSON 数组，禁止在 JSON 前后添加任何分析、说明、解释文字
- 不要用 markdown 代码块包裹。
- 同一对话+同一标签只需输出一个对象，每个对象单独一行
- 列出最多三个关键依据的消息line序号数组
- {{output_format}}


# 规则：
- 按对话序号依次逐条分析判断，不可遗漏不可乱造
- 仅根据客户明确表达的意图判断，不要推测隐含意图
- ⚠️ 对话line序号严禁越界：每个对话标注了该对话的line序号范围（如"line范围:1-3"），line数组中的每个数字都必须在该范围内。反例：对话标注"line范围:1-2"却返回line:[15,16,17]是严重错误，因为该对话只有2条消息
{{match_rule}}


# 对话列表：
{{dialogues_text}}



